import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-input-page-transition-bug',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
